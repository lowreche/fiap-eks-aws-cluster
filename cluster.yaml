apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: fiap-eks-aws-cluster
  region: us-east-1
  version: "1.31"

managedNodeGroups:
  - name: standard-nodes
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 1
    maxSize: 3
    volumeSize: 20
